#todo fix this

version: '{branch} - {build}'

branches:
  only:
    - master
    - develop


environment:
  GOPATH: c:\gopath
  GOVERSION: 1.10

install:
  - go get -u github.com/golang/dep/...
  - dep ensure


build_script:
  
  - go test ./..
  - go build cmd/gosint/main.go

artifacts:
  - path: gosint  